[workspace]
default-members = ["cli"]
members = [
    "cli",
    "core",
    "macros",
    "reference",
    "scripts",
    "web",
]

resolver = "2"

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
